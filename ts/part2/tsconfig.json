{
//  ts的json可以写注释
/*
*  tsconfig.json是ts编译器的配置文件，ts编译器可以根据它的信息来对代码进行编译
*  "include" 用来指定哪些ts文件需要被编译
   路径： * 表示任意文件; ** 表示任意目录
*  "exclude" 不需要被编译的文件目录
  
   */ 
  "include": [
    "./src/**/*"
  ],
  "exclude": [
    "./src/hello/**/*"
  ]
}